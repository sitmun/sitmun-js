import {WorkspaceApplication} from "../workspaceapplication/workspaceapplication";

export type MockWorkspaceApplication = {
    username?: string,
    applicationId: number,
    territoryId: number
    workspaceApplication: WorkspaceApplication
}